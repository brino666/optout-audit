PORT=3000

# Set to "true" once your Stripe keys below are filled in and you're ready
# to actually charge for scans. Leave "false" for local development/testing.
REQUIRE_PAYMENT=false

# From your Stripe Dashboard → Developers → API keys.
# Secret key stays server-side only — never expose this in front-end code.
STRIPE_SECRET_KEY=sk_test_xxx

# Price per scan, in cents. 900 = $9.00
SCAN_PRICE_CENTS=900
SCAN_CURRENCY=usd

# CIPA add-on price, in cents. 700 = $7.00 (tier 2 total: $16.00)
CIPA_ADDON_CENTS=700
